# Copié automatiquement en .env au premier lancement (demarrer-projet.bat)
# ou copie manuelle : copy env.docker.template .env

DATABASE_URL=postgresql://nutrifish:nutrifish@localhost:5433/nutrifish_gmao?schema=public

# NextAuth (plus tard)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
